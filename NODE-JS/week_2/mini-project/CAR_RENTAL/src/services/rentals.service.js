function getAllRentalsService(filters) {
  // Implémentation vide
}

function getRentalByIdService(id) {
  // Implémentation vide
}

function createRentalService(rentalData) {
  // Implémentation vide
}

function returnRentalService(id) {
  // Implémentation vide
}

function cancelRentalService(id) {
  // Implémentation vide
}

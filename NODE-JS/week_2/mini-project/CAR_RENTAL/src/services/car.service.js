function getAllCarsService(filters) {
  // Implémentation vide
}

function getCarByIdService(id) {
  // Implémentation vide
}

function createCarService(carData) {
  // Implémentation vide
}

function updateCarService(id, carData) {
  // Implémentation vide
}

function deleteCarService(id) {
  // Implémentation vide
}
